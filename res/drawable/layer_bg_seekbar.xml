<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- 背景图 -->
	<item android:id="@+android:id/background" android:drawable="@android:color/white" />
	<!-- 第二进度图 -->
	<item android:id="@+android:id/SecondaryProgress" android:drawable="@android:color/black" />
	<!-- 进度 -->
	<item android:id="@+android:id/progress" android:drawable="@android:color/black" />
</layer-list>